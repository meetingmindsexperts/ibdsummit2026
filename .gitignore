# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Dependencies (none required, but just in case)
node_modules/

# Logs / temp
*.log
.ftp-deploy-sync-state.json

# Local env / secrets — never commit
.env
.env.*

# Raw font source downloads (we ship only the optimized assets/fonts/*.woff2)
assets/Johnson Display/
assets/JOHNSONTEXT Regular.ttf

# Playwright test artifacts
.playwright-mcp/
*.playwright.png

# Server-side secrets (written at deploy from GitHub secrets)
includes/secrets.php

# Runtime logs (keep the dir, ignore the files)
storage/logs/*.log

# Client source / reference docs — keep locally, never publish
/Nursing Training Progam.pdf
/The IL-23 era for IBD ( UAE Experience Exchange ).pdf
