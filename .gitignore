# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Dependencies (none required, but just in case)
node_modules/

# Logs / temp
*.log
.ftp-deploy-sync-state.json

# Local env / secrets — never commit
.env
.env.*

# Raw font source downloads (we ship only the optimized assets/fonts/*.woff2)
assets/Johnson Display/
assets/JOHNSONTEXT Regular.ttf

# Playwright test artifacts
.playwright-mcp/
*.playwright.png
